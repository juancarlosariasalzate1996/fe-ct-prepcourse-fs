/*⚠️ NO MODIFIQUES EL NOMBRE DE LAS DECLARACIONES ⚠️*/
/*2️⃣ EJERCICIO 02 2️⃣*/

function devolverString(string) {
   // Debe retornar un string.
    return string;
}

// ⛔️ "X" e "Y" son números.

function suma(x, y) {
varx = 10;// Retorna el resultado de su suma.
vary = 16;// Tu código:
return x + y;
};

function resta(x, y) {
  varx = 10; // Retorna el resultado de la resta.
vary = 16;// Tu código:
return x - y;
}

function divide(x, y) {
varx = 16;   // Retorna el resultado de su división.
vary = 4; // Tu código:
return x / y;
}

function multiplica(x, y) {
  varx = 6;// Retorna el resultado de su multiplicación.
  vary = 11;// Tu código:
return x * y;
}

function obtenerResto(x, y) {
 varx = 21;  // Obten el resto de la división de "x" entre "y".
   vary = 5;// Tu código:
return x % y;
}

/*⚠️ NO MODIFIQUES NADA DEBAJO DE ESTO ⚠️*/
module.exports = {
   devolverString,
   suma,
   resta,
   divide,
   multiplica,
   obtenerResto,
};
